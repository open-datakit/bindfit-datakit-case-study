def main(
    data,
    fitMethod,
    fitModel,
    fitModelParams,
    subInitValues,
    dilutionCorrection,
    flavour,
    fitResults,
):
    print(data)

    #if not data["data"]:
    #    raise ValueError("No data passed to algorithm")


    return {
        "flavour": "test"
    }
